# Proomo-early-access

This repository contains a single-page coming-soon design for ProOmo (HTML + CSS).

Files:
- `proomo.html` — the main HTML page
- `css/styles.css` — styles for the page

Notes:
- `it.html` is excluded in `.gitignore` per request.
